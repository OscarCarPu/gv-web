name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          git config --global --add safe.directory /home/ocp/docker/gv/gv-web
          cd /home/ocp/docker/gv/gv-web
          git pull
          make up
