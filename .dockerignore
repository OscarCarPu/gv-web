# Installed inside the image by `bun install`; copying the host's tree over it
# both bloats the context and replaces the platform's own binaries.
node_modules

# Build output and caches, regenerated by the build
.svelte-kit
build
.output

# Runtime data and secrets
data
.env
.env.*
!.env.example

.git
.gitea
